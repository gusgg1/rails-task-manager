<%= simple_form_for(@task) do |f| %>
  <%= f.input :title %>
  <%= f.text_area :details %>
  <%= f.check_box :completed %>
  <div>
    <%= f.submit %>
  </div>
<% end %>

<%= link_to "Back to list", tasks_path %>
